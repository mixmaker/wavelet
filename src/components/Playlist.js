import React from "react";
import styled from "styled-components";

const Playlist = () => {
  return <StyledPlaylist>Coming Soon...</StyledPlaylist>;
};

const StyledPlaylist = styled.div`
  position: relative;
  margin: 2rem ;
  /* left: 20%; */
`;

export default Playlist;
